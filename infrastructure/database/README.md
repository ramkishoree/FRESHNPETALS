# Database Infrastructure

Supabase/Postgres migrations, seeds, and RLS policies (Phase 3).

- `migrations/` — versioned SQL migrations (source of truth for schema)
- `seeds/` — seed data for local/dev/staging environments
